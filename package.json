{
  "name": "boyoticom-erp",
  "version": "1.0.0",
  "private": true,
  "description": "Boyoticom ERP Static Site",
  "scripts": {
    "build": "mkdir -p dist && cp *.html dist/ && cp *.json dist/ && (cp *.png dist/ || true)"
  },
  "dependencies": {},
  "devDependencies": {}
}
